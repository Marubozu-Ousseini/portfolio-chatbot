name: Precompute RAG from S3 (disabled)

# This workflow has been intentionally disabled. The RAG precompute and upload
# should be performed manually (e.g., via deploy.sh) or through on-demand scripts.

on:
  # Keep only a manual trigger, with the job force-disabled below.
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "precompute-rag workflow disabled"
